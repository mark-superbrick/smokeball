{
  "name": "smokeball",
  "version": "1.0.0",
  "private": true,
  "description": "Plain-JS Webflow scripts for Smokeball, shipped via jsDelivr CDN.",
  "scripts": {
    "dev": "browser-sync start --config bs-config.js",
    "dev:open": "browser-sync start --config bs-config.js --open"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2"
  }
}
